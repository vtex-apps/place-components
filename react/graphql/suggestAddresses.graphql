query SuggestAddresses($searchTerm: String) {
  suggestAddresses(searchTerm: $searchTerm) {
    description
    mainText
    mainTextMatchInterval {
      offset
      length
    }
    secondaryText
    externalId
  }
}
