query GetAddressByExternalId($id: String!, $sessionToken: String) {
  getAddressByExternalId(id: $id, sessionToken: $sessionToken)
    @context(provider: "vtex.geolocation-graphql-interface") {
    addressId
    addressType
    city
    complement
    country
    geoCoordinates
    neighborhood
    number
    postalCode
    reference
    state
    street
  }
}
